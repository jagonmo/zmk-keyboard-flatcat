# Right half — peripheral. Runs effects only; no host link, no overlays.
# Effect, hue, brightness and speed arrive from the central because the
# &rgb_pro behaviour is declared BEHAVIOR_LOCALITY_GLOBAL.
